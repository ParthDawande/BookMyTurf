PHASE 9-admin-foundation-and-dashboard — AdminNav + admin dashboard
at /admin/dashboard. Smaller of the two admin sub-phases; the
operations sub-phase follows.

Standing rules apply. Stop and report. Do NOT start
9-admin-operations. Reuse Phase 0 error shape, Phase 2 security
UNCHANGED. The backend is frozen — build against existing
endpoints. No new dependencies.

SCOPE: two things in this sub-phase.
- AdminNav component (Dashboard / Approvals / Users / Staff /
  Complaints / Queries / Notifications).
- Admin dashboard at /admin/dashboard wired to the 8B-admin
  endpoint.

Same shape as 9-owner-foundation-and-dashboard. Reuses the
NotificationsPage component with the Nav prop (now AdminNav). The
/admin stub from 9-foundation becomes a redirect to
/admin/dashboard.

=== Backend endpoints used (existing, do not modify) ===
- GET /api/admin/dashboard?from_date=&to_date= (8B-admin endpoint
  with platform-wide aggregations, snapshot fields, dual-domain
  date filter per the 8B-admin contract).
- GET /api/notifications (8C, role-agnostic, already wired in
  9-customer-other's NotificationsPage).

Verify the admin dashboard response shape via curl before
building. The 8B-admin contract returned 17 fields when we
verified it; confirm they're all still there:
- total_revenue, platform_commission, owners_payout_total
- bookings_by_status (CONFIRMED/COMPLETED/CANCELLED/REFUNDED)
- payouts_summary (PENDING/PAID/CANCELLED)
- pending_approvals.turfs, pending_approvals.sub_courts
- open_complaints, open_queries
- active_users.customers, active_users.owners, active_users.staff
- filters echo

=== Locked design decisions ===
- Q2 (carried from 9-owner): admin lands at /admin/dashboard
  post-login. The /admin stub from 9-foundation becomes a
  redirect to /admin/dashboard.
- KPI-card dashboard layout (no charting library). Same shape
  as owner dashboard with two extra row groups for the snapshot
  fields specific to admin (pending approvals, open
  complaints/queries, active users).

=== AdminNav component ===

Horizontal sub-nav below the existing Header. Items:
- Dashboard → /admin/dashboard (active in this sub-phase).
- Approvals → /admin/approvals (stub, "Coming in 9-admin-
  operations").
- Users → /admin/users (stub).
- Staff → /admin/staff (stub).
- Complaints → /admin/complaints (stub).
- Queries → /admin/queries (stub).
- Notifications → /admin/notifications (uses reusable
  NotificationsPage with AdminNav prop).

The Nav prop pattern established in 9-customer-other and used
correctly in 9-owner-foundation-and-dashboard applies here:
NotificationsPage accepts <NavigationComponent /> as a prop;
admin mounts it with AdminNav.

=== /admin/dashboard page ===

Layout:
- Header + AdminNav with Dashboard active.
- Page title: "Admin Dashboard."
- Date range filter at the top: from_date / to_date snake_case
  URL params. Same shape as owner dashboard.

Main content — three sections:

SECTION 1: Financial overview (KPI cards, 3-column on desktop):
- Total Revenue: ₹X (large).
- Platform Commission: ₹X (the platform's earned commission —
  what the business itself earned, not the gross).
- Owners' Total Payout: ₹X.

SECTION 2: Operational snapshot (NOT subject to date filter):
A clearly labeled "Current State" section with a small note:
"These reflect the current operational state, not the date range."

Two rows of cards:

Row 2a — Pending approvals:
- Turfs awaiting approval: N (clickable → /admin/approvals,
  stub for now).
- Sub-courts awaiting approval: N (clickable → /admin/approvals).

Row 2b — Open tickets:
- Open complaints: N (clickable → /admin/complaints, stub).
- Open queries: N (clickable → /admin/queries, stub).

Row 2c — Active users:
- Customers: N
- Owners: N
- Staff: N

SECTION 3: Booking + payout state (subject to date filter,
same as owner dashboard):

Row 3a — Bookings by status:
CONFIRMED / COMPLETED / CANCELLED / REFUNDED (4 mini-cards,
color-coded per status).

Row 3b — Payouts summary:
Pending / Paid / Cancelled (3 mini-cards, color-coded).

States: loading skeleton, error banner, empty (all zeros render
cleanly — admin starting from scratch wouldn't have meaningful
data, but verify it renders).

URL semantics: /admin/dashboard?from_date=&to_date= snake_case,
bookmarkable, refresh-preservable.

=== Carried-forward contracts ===

1. The 8B-admin contract: snapshot fields (pending_approvals,
   open_complaints, open_queries, active_users) have NO date
   filter applied. Display them with a small label explaining
   why ("current state, not date-filtered"). The dual-domain
   rule from 8B-owner applies here too: bookings filtered by
   booking_date, payouts by scheduled_at.
2. Snake_case throughout.
3. Phase 2 routing: customer/owner/staff can't reach
   /admin/dashboard → redirect to / per RoleGuard.
4. The parallel-fetch pattern from 9-owner-finance-and-engagement
   (dashboard + profile in parallel on mount) MAY apply here if
   admin has an analogous "profile" need. For v1 we just fetch
   the dashboard; admin doesn't have the bank-details kind of
   completion check that owners do.

=== Do NOT in this sub-phase ===
- No approval queues (9-admin-operations).
- No user/staff management.
- No complaints/queries lists.
- No backend changes.
- No new dependencies (no charting library).

=== VERIFICATION via Playwright + dev server ===

Use admin@bookmyturf.in / Test@1234 from earlier phases. Test
data state from 8B-admin acceptance: 28 total bookings across
2 active owners, 17800 revenue, 1780 commission (this has grown
since 8B-admin landed — booking 33/34/35 etc were added during
9-customer-* sub-phases), 3 pending turfs, 4 pending sub-courts
(approximately — verify against the live numbers).

1. Files created/modified. State the admin dashboard response
   shape verified via curl, side-by-side with what 8B-admin
   shipped — confirm all 17 fields still present. State whether
   /admin redirects to /admin/dashboard.

2. ADMIN LOGIN:
   a. Log in as admin. Land on /admin/dashboard (not /admin
      stub).
   b. AdminNav visible with Dashboard active.

3. DASHBOARD — NO FILTER:
   a. GET /api/admin/dashboard fires. Show response.
   b. KPI cards render field-by-field with response data.
      State each card's value side-by-side with the response
      field (same discipline as 9-owner-foundation-and-dashboard
      item 4b).
   c. Section 1: revenue/commission/payout cards.
   d. Section 2: pending_approvals, open_complaints, open_queries,
      active_users cards. Each with the "current state" label.
   e. Section 3: bookings_by_status (4 cards) and
      payouts_summary (3 cards) with color coding.

4. SNAPSHOT INVARIANT — explicitly verify:
   a. With no date filter: capture values of pending_approvals,
      open_complaints, open_queries, active_users.
   b. Apply date filter (?from_date=2026-08-01&to_date=2026-08-31).
      Network refetches.
   c. STATE that the snapshot fields are UNCHANGED in the
      filtered response (the carried-forward 8B-admin rule).
   d. The financial cards and bookings/payouts DID change.
      Show the numbers before and after.

5. DATE FILTER ROUND-TRIP:
   - Apply filter. URL updates with snake_case params.
   - Refresh page. Filter preserved, refetch fires.
   - Clear filters. URL params removed. Numbers restore.

6. ADMINNAV navigation:
   a. Click each nav item:
      - Dashboard → /admin/dashboard (current).
      - Approvals → /admin/approvals (stub).
      - Users → /admin/users (stub).
      - Staff → /admin/staff (stub).
      - Complaints → /admin/complaints (stub).
      - Queries → /admin/queries (stub).
      - Notifications → /admin/notifications (NotificationsPage
        with AdminNav).
   b. State that /admin/notifications shows AdminNav (NOT
      OwnerNav or CustomerNav).

7. ROLE GUARDS:
   a. Customer → /admin/dashboard redirects to /.
   b. Owner → /admin/dashboard redirects to /.
   c. Staff → /admin/dashboard redirects to /.
   d. Anonymous → /login.

8. CROSS-PORTAL CONSISTENCY:
   - Verify admin dashboard numbers match what 8B-admin tested
     (modulo growth from 9-customer-* booking activity).
   - Specifically: bookings_by_status counts should be the SUM
     of all owners' counts (admin sees platform-wide), so
     admin.bookings_by_status.CONFIRMED should equal owner-20-
     dashboard.bookings_by_status.CONFIRMED + owner-32-
     dashboard.bookings_by_status.CONFIRMED + any other owner's
     CONFIRMED count.
   - Spot-check this with one status. State the math.

9. NETWORK PAYLOADS: all snake_case throughout.

10. MOBILE: dashboard sections stack vertically on ~375px width.

Do not begin 9-admin-operations.